# 系统功能结构图

## 1 技术架构总览

### 1.1 整体技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | Vue 2.7 + Element UI | AI 对话页面、知识库管理弹窗 |
| 后端框架 | SpringBoot 2.6 | 业务逻辑、权限校验、数据持久化 |
| AI 服务 | FastAPI (Python) | RAG 检索、LLM 对话、用例生成 Agent |
| 向量数据库 | Chroma | 知识文档向量存储 |
| 大语言模型 | DeepSeek / Qwen / OpenAI | 智能对话与用例生成 |
| Embedding | Ollama / OpenAI | 文档向量化 |

### 1.2 系统整体架构图

```mermaid
flowchart TB
    subgraph 前端[Vue 前端应用]
        A1[AI 对话页面]
        A2[知识库管理弹窗]
        A3[用例预览编辑组件]
        A4[localStorage 对话历史]
    end

    subgraph Java后端[SpringBoot 后端服务]
        B1[AiController]
        B2[AiService]
        B3[权限校验模块]
        B4[数据持久化]
        B5[Schema 抽取]
    end

    subgraph AI服务[FastAPI AI 服务]
        C1[路由层 routers]
        C2[Agent 服务]
        C3[RAG 服务]
        C4[LLM 服务]
        C5[工具模块 tools]
    end

    subgraph 外部依赖[外部服务]
        D1[(MySQL 数据库)]
        D2[(Chroma 向量库)]
        D3[LLM Provider]
        D4[Embedding 服务]
    end

    A1 -->|HTTP + SSE| B1
    A2 -->|HTTP| B1
    A3 -->|HTTP| B1
    B1 --> B2
    B2 --> B3
    B2 --> B4
    B2 --> B5
    B2 -->|HTTP/REST| C1
    B4 -->|JDBC| D1
    C1 --> C2
    C1 --> C3
    C1 --> C4
    C2 --> C5
    C3 -->|向量检索| D2
    C4 -->|API调用| D3
    C3 -->|Embedding| D4
    C5 -->|HTTP| B1
```

---

## 2 核心功能模块

### 2.1 模块详情结构图

```mermaid
flowchart LR
    subgraph 前端模块
        FE1[AI 对话模块]
        FE2[知识库管理模块]
        FE3[用例生成模块]
    end

    subgraph 后端模块
        BE1[AiController]
        BE2[AiService]
        BE3[权限校验]
        BE4[数据持久化]
    end

    subgraph AI服务模块
        AI1[路由层]
        AI2[AgentService]
        AI3[LLMService]
        AI4[RAGService]
        AI5[PlatformClient]
    end

    FE1 --> BE1
    FE2 --> BE1
    FE3 --> BE1
    BE1 --> BE2
    BE2 --> BE3
    BE2 --> BE4
    BE1 --> AI1
    AI1 --> AI2
    AI1 --> AI3
    AI1 --> AI4
    AI2 --> AI5
```

### 2.2 前端模块

| 模块 | 职责 | 核心功能 |
|------|------|----------|
| AI 对话页面 | 用户与 AI 交互 | SSE 流式接收、对话历史管理 |
| 知识库管理弹窗 | 文档 CRUD | 目录树展示、新建/编辑/删除、重建索引 |
| 用例预览编辑组件 | 生成结果处理 | JSON 解析、表单填充、手动保存 |

### 2.3 后端模块

| 模块 | 职责 | 核心功能 |
|------|------|----------|
| AiController | HTTP 请求入口 | 路由分发、请求参数校验 |
| AiService | 业务逻辑编排 | AI 服务调用、数据持久化协调 |
| 权限校验 | 项目/用户权限 | 项目访问控制、文档操作权限 |
| 数据持久化 | MySQL 操作 | 知识文档增删改查 |

### 2.4 AI 服务模块

| 模块 | 职责 | 核心功能 |
|------|------|----------|
| routers | 请求路由 | chat、knowledge、agent 路由分发 |
| AgentService | 对话与用例生成 | 智能分流、ReAct Agent、Case 生成 |
| LLMService | 大模型交互 | 流式/非流式对话、Provider 切换 |
| RAGService | 知识检索 | 文本分块、向量检索、混合搜索 |
| PlatformClient | 平台 API 调用 | 接口列表/详情获取、Schema 抽取 |

---

## 3 数据流转图

### 3.1 AI 对话流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant BE as SpringBoot
    participant AI as FastAPI
    participant LLM as 大语言模型
    participant RAG as Chroma

    U->>FE: 输入问题
    FE->>BE: POST /ai/chat/stream
    BE->>AI: POST /ai/chat/stream
    AI->>RAG: 检索知识库
    RAG-->>AI: 返回知识片段
    AI->>LLM: 构建 Prompt
    LLM-->>AI: 流式响应
    AI-->>BE: SSE 事件流
    BE-->>FE: SSE 事件流
    FE->>U: 实时展示回答
```

### 3.2 知识库索引流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant BE as SpringBoot
    participant AI as FastAPI
    participant RAG as Chroma

    U->>FE: 点击重建索引
    FE->>BE: POST /ai/knowledge/index/{id}
    BE->>AI: POST /ai/rag/add
    AI->>AI: 文本分块
    AI->>RAG: 向量化存储
    RAG-->>AI: 索引结果
    AI-->>BE: 返回索引状态
    BE->>BE: 更新 MySQL 状态
    BE-->>FE: 返回成功
    FE->>U: 提示索引完成
```

### 3.3 用例生成流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant BE as SpringBoot
    participant AI as FastAPI
    participant LLM as 大语言模型
    participant PLAT as 平台API

    U->>FE: 输入用例需求
    FE->>BE: POST /ai/generate/case
    BE->>AI: POST /ai/agent/generate-case
    AI->>PLAT: 获取接口列表
    PLAT-->>AI: 接口列表
    AI->>AI: ReAct 决策选择接口
    AI->>PLAT: 获取接口详情
    PLAT-->>AI: 接口详情
    AI->>LLM: 生成用例 JSON
    LLM-->>AI: 用例内容
    AI-->>BE: 返回用例
    BE-->>FE: 返回用例预览
    FE->>U: 展示预览
    U->>FE: 编辑并保存
    FE->>BE: POST /ai/generate/case/save
    BE->>BE: 校验 apiId
    BE->>BE: 保存到 MySQL
    BE-->>FE: 返回成功
```

---

## 4 技术架构说明

### 4.1 服务间通信

| 通信链路 | 协议 | 格式 | 特性 |
|----------|------|------|------|
| 前端 → 后端 | HTTP | JSON | RESTful API |
| 后端 → AI 服务 | HTTP | JSON | RESTful API |
| AI → 平台 API | HTTP | JSON | 内部调用 |
| AI → LLM | HTTP | JSON | API 调用 |
| SSE 流式 | HTTP + SSE | text/event-stream | 长连接、流式输出 |

### 4.2 SSE 事件协议

```json
// 文本增量
{"type": "content", "delta": "这是新增的文本..."}

// 用例生成
{"type": "case", "case": {...}, "api_ids": ["123", "456"]}

// 错误事件
{"type": "error", "message": "错误信息"}

// 正常结束
{"type": "end"}
```

### 4.3 配置管理

| 配置项 | 位置 | 说明 |
|--------|------|------|
| AI 服务地址 | application.yml | `ai.service.base-url` |
| LLM Provider | config.yaml | `llm.provider` |
| Embedding 模型 | config.yaml | `embedding.provider` |
| 向量库路径 | config.yaml | `vector_store.persist_directory` |

### 4.4 部署架构

```mermaid
flowchart TB
    subgraph 内部网络
        subgraph 应用服务
            FE[前端服务:5173]
            BE[后端服务:8080]
        end
        subgraph AI 服务
            AI[FastAPI:8001]
        end
        DB[(MySQL:3306)]
    end

    subgraph 可选外部服务
        CHROMA[Chroma 向量库]
        LLM[DeepSeek/Qwen API]
        EMB[Ollama Embedding]
    end

    FE --> BE
    BE --> AI
    BE --> DB
    AI --> CHROMA
    AI --> LLM
    AI --> EMB
```
